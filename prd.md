# Virtual Cigarette Interaction System — Product Requirements Document

업데이트: 2026-08-22

## 1. 제품 목표

웹캠 위에서 담배를 집어 입에 대고, 실제 흡연 동작처럼 입술을 오므리고 입을 여는 짧고 명확한 AR 인터랙션을 제공한다. 영상과 랜드마크는 브라우저 밖으로 전송하지 않는다.

핵심 흐름은 다음 하나다.

> 담배를 입에 댄다 → 입술을 오므린 시간만큼 담배가 탄다 → 입을 열면 `#696969` 회색 연기가 한 번에 나온다 → 3초 동안 입을 열지 않으면 양쪽 코로 한 번에 나온다.

## 2. 인터랙션 사양

### 2.1 담배 집기와 입 부착

- 엄지·검지 핀치 또는 검지·중지 집기로 담배를 잡는다.
- 담배를 입 근처에서 놓으면 왼쪽, 중앙 또는 오른쪽 입술에 부착한다.
- 담배 기본 길이와 두께는 1/3 축소 이전의 원래 크기로 복구한다.
- 시각 크기와 관계없이 집기 판정 반경은 기존 수준을 유지한다.

### 2.2 흡입과 연소

- 담배가 입에 부착되어 있거나 손에 들린 담배가 입 근처에 있을 때, 입 가로 폭이 얼굴 폭의 31% 이하로 좁아지면 입술 오므림으로 본다.
- 손에 든 담배는 입에 놓아 고정하지 않아도 입 근처에서 오므리면 즉시 동일한 흡입·연소가 시작된다.
- 입술을 오므린 동안만 `INHALING` 상태가 된다.
- 흡입 중에만 불씨가 밝아지고 담배 끝에서 약한 회색 연기가 나온다.
- 담배 길이는 누적 흡입 시간에 정비례해 연속적으로 줄어든다.
- 1회 평균 흡입을 1.4초로 보고, 약 7회인 총 9.8초의 누적 흡입으로 완전히 소진한다.
- 소진 시 담배는 입에서 분리되어 짧게 위로 튄 뒤 회전하며 화면 아래로 떨어진다.

### 2.3 입 연기

- 0.25초 이상의 유효한 흡입이 끝나면 연기 1회를 대기한다.
- 대기 상태에서 입을 열면 입 중앙에서 `#696969` 회색 연기를 하나의 큰 배치로 즉시 생성한다.
- 입을 계속 열고 있어도 반복 생성하지 않으며, 한 번의 유효한 흡입당 정확히 한 번만 분출한다.
- 입 연기 입자 수는 기존 단발 기준인 280~400개의 정확히 5배인 약 1,400~2,000개로 만든다.

### 2.4 코 연기

- 흡입이 끝난 뒤 3초 동안 입을 열지 않으면 입 연기 대신 양쪽 콧구멍에서 `#696969` 회색 연기를 하나의 큰 배치로 즉시 생성한다.
- 코 연기 입자 수는 기존 단발 기준인 콧구멍당 70~94개의 정확히 5배인 약 350~470개로 만든다.
- 코 연기가 나온 뒤 같은 흡입으로 입 연기가 다시 나오지 않는다.

## 3. 단순 상태 흐름

런타임은 아래 상태만 사용한다.

1. `IDLE` 또는 담배 이동
2. `CIGARETTE_IN_MOUTH`
3. `INHALING`
4. `SMOKE_READY`
5. `MOUTH_BURST` 또는 `NOSE_BURST`
6. 담배 소진 시 `CIGARETTE_FALLING`

흡입 저장량 배열, 연속 분출 타이머, 입 열림 피크, 입술 수축 이력 같은 별도 상태는 두지 않는다.

## 4. 렌더링과 성능

- 담배와 연기는 Three.js WebGL `high-performance` 렌더러로 합성한다.
- 카메라의 `requestVideoFrameCallback`이 전달한 최신 프레임만 사용하며 오래된 프레임 큐를 만들지 않는다.
- 인위적인 FPS 제한 없이 카메라 새 프레임마다 하나의 추론만 동기식으로 실행한다.
- 손 추적을 우선하는 `HAND → HAND → FACE` 순서로 실행해 손 입력이 얼굴·효과 처리 뒤에서 기다리지 않게 한다.
- MediaPipe GPU 태스크에는 `HTMLVideoElement`를 직접 전달하고 중간 2D 캔버스 `drawImage` 복사를 하지 않는다.
- 추적 콜백과 화면 `requestAnimationFrame` 렌더링을 분리하며, 렌더러는 가장 최근 추적 결과만 소비한다.
- 연기는 단일 `ShaderMaterial`과 `Points`로 GPU에서 렌더링한다.
- 입·코·별 연기는 각각 한 번의 GPU 파티클 배치로 만든다.
- 연기 색상은 셰이더의 노이즈나 밝기 보간 없이 `#696969` (`rgb(105, 105, 105)`)로 고정한다.
- 8,000개 GPU 파티클 버퍼와 빠른 품질 전환으로 5배 연기량과 실시간 반응을 함께 유지한다.
- 개별 물방울 모양 대신 절차적 노이즈 가장자리를 가진 반투명 연무로 보이게 한다.
- 얼굴·손 추적은 MediaPipe GPU delegate를 우선하고 실패할 때만 CPU로 대체한다.
- 디버그가 꺼진 동안 랜드마크 캔버스, 상태 스토어와 DOM 진단값을 매 프레임 갱신하지 않는다.
- 평균 FPS와 함께 최근 240프레임의 최악 프레임 시간, p95 프레임 시간, 손 입력 지연 및 손·얼굴 추론 시간을 진단한다.

## 5. 수용 기준

- [ ] 담배가 1/3 축소 이전의 크기로 복구되어 보인다.
- [ ] 입에 물거나 손에 들고 입 근처에 둔 담배는 입술을 오므린 시간만큼 연속적으로 짧아진다.
- [ ] 약 1.4초씩 7번, 총 9.8초 흡입하면 담배가 소진되어 아래로 떨어진다.
- [ ] 흡입 중에만 불씨와 약한 담배 끝 연기가 보인다.
- [ ] 흡입 후 입을 열면 `#696969` 입 연기가 한 번에 한 배치만 나온다.
- [ ] 입·코 연기의 입자 수가 기존 단발 기준의 정확히 5배다.
- [ ] 볼 터치와 별 연기 관련 판정·상태·렌더링 코드가 존재하지 않는다.
- [ ] 흡입 후 3초 동안 입을 열지 않으면 양쪽 코에서 `#696969` 연기가 한 번에 한 배치만 나온다.
- [ ] GPU 진단, TypeScript 검사, 프로덕션 빌드와 자동 회귀 테스트가 통과한다.
- [ ] 추적은 최신 비디오 프레임만 처리하며 동시에 둘 이상의 추론을 실행하거나 프레임을 대기열에 넣지 않는다.
- [ ] 손 추론이 얼굴 추론보다 두 배 자주 우선 실행되고, GPU 입력 전 캔버스 복사가 없다.
- [ ] 디버그 화면에서 평균 FPS, 최악/p95 프레임 시간과 입력 지연을 확인할 수 있다.
